interface:
  display_name: "Maintain K-Tech Catalog"
  short_description: "Refresh and audit the official-source knowledge index"
  default_prompt: "Use $maintain-k-tech-catalog to refresh and audit the K-Tech Radar catalog."

dependencies:
  tools:
    - type: "mcp"
      value: "k-tech-radar"
      description: "Local source-partitioned tech-blog catalog and Dictionary"

policy:
  allow_implicit_invocation: true
